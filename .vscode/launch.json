{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Start WarmChats",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}/src",
      "preLaunchTask": "Dev: Start all servers"
    }
  ]
}
